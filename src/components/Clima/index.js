import { StyleSheet, Text, View } from "react-native";

export default function Clima() {
    return(
        <View style={styles.ele}></View>
    );

}


const styles = StyleSheet.create({
    ele:{
        height: '70%',
        width: '100%',
    }
})